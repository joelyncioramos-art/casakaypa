# Solo la website va al repo: se ignora todo lo demás
# (carpetas de fotos/videos crudos de 2.2 GB, configuración local, etc.)
/*

# Excepciones: lo que SÍ se versiona
!/site
!/design-system
!/.gitignore
!/README.md
!/.github
!/.github/**
